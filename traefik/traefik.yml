entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

certificatesResolvers:
  letsencrypt:
    acme:
      email: john@doe.com
      storage: acme.json
      httpChallenge:
        entryPoint: web

providers:
  docker: {}

api:
  dashboard: true
